<%= search_form_for @q do |f| %>
  <%= f.label :region_id_eq, "Region" %>
  <%= f.collection_select :region_id_eq, Region.all, :id, :name, {}, { include_blank: true, class: 'form-control mb-2' } %>
  <%= f.submit 'Search', class: 'btn btn-primary' %>
<% end %>